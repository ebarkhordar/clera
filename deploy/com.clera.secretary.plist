<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN"
  "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  launchd job for Clera on macOS: survives reboots, restarts on crash.

  Install (adjust CLERA_DIR first):
    sed "s|CLERA_DIR|$HOME/CodingProjects/clera|g" deploy/com.clera.secretary.plist \
      > ~/Library/LaunchAgents/com.clera.secretary.plist
    launchctl load ~/Library/LaunchAgents/com.clera.secretary.plist

  Logs: CLERA_DIR/data/clera.log — status: launchctl list | grep clera
-->
<plist version="1.0">
<dict>
  <key>Label</key>
  <string>com.clera.secretary</string>
  <key>WorkingDirectory</key>
  <string>CLERA_DIR</string>
  <!-- caffeinate -s keeps the Mac awake while on AC power, so the collector
       doesn't stop when the display sleeps. On battery, closing the lid still
       sleeps the machine — a laptop is not an always-on host; use the Docker
       or systemd recipe on a server for true 24/7. -->
  <key>ProgramArguments</key>
  <array>
    <string>/usr/bin/caffeinate</string>
    <string>-s</string>
    <string>CLERA_DIR/.venv/bin/python</string>
    <string>-m</string>
    <string>app.main</string>
  </array>
  <key>RunAtLoad</key>
  <true/>
  <key>KeepAlive</key>
  <true/>
  <key>ThrottleInterval</key>
  <integer>15</integer>
  <key>StandardOutPath</key>
  <string>CLERA_DIR/data/clera.log</string>
  <key>StandardErrorPath</key>
  <string>CLERA_DIR/data/clera.log</string>
</dict>
</plist>
